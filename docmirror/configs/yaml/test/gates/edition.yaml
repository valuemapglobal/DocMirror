# TQG edition track — PEC / DEC schema gates
version: 1
track: edition
layer: pec

cases:
  - id: wechat_community_records_parity
    tier: regression
    tier_tags: [slow]
    fixture: fixtures/wechat_payment/DemoUser+微信流水.pdf
    pipeline: edition
    editions: [community]
    options:
      enhance_mode: standard
    gates:
      edition_total_rows: { min: 5111 }
      edition_drift_ratio: { max: 0.005 }
    tags: [cashflow, wechat, community]

  - id: wechat_community_dec_validation
    tier: regression
    fixture: fixtures/wechat_payment/DemoUser+微信流水.pdf
    pipeline: edition
    editions: [community]
    options:
      enhance_mode: standard
      max_pages: 5
    gates:
      dec_validation: { max_issues: 0 }
    tags: [cashflow, wechat, community, smoke_subset]
