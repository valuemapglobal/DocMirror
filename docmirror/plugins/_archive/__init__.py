"""
Archived community plugins (pre 6+1 strategy).

Legacy domain plugins retained for reference and migration tests. Not registered by
``plugin_registry`` discovery and not shipped in wheel builds. Superseded by the
six premium community domains plus ``generic`` fallback.

Pipeline role: none in production — historical implementations only.
"""
