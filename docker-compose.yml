services:
  docmirror:
    build:
      context: .
      dockerfile: Dockerfile
    image: docmirror:latest
    container_name: docmirror
    ports:
      - "8000:8000"
    volumes:
      - docmirror_cache:/root/.cache
      - docmirror_data:/root/.docmirror
    environment:
      # Optional: Enable full enhancement mode for parsing
      - DOCMIRROR_ENHANCE_MODE=standard
      - DOCMIRROR_LOG_LEVEL=info
      # Optional: Control concurrency
      - OMP_NUM_THREADS=4
      # Optional: API authentication key
      # - DOCMIRROR_API_KEY=your-secret-key
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    # Optional: If you intend to run GPU-accelerated OCR in the future
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]

volumes:
  docmirror_cache:
  docmirror_data:
